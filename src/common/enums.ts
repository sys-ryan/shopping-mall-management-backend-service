export enum COUPONE_TYPE_ENUM {
  FIXED_VALUE = "fixed_value",
  PERCENTAGE = "percentage",
  SHIPPING_FEE = "shipping_fee",
}

export enum DELIVERY_STATUS_ENUM {
  SHIPPING = "shipping",
  COMPLETED = "completed",
}

export enum PAY_STATE_ENUM {
  COMPLETED = "completed",
  CANCELED = "canceled",
}
